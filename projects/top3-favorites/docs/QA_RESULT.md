# top3-favorites QA結果（静的確認）

実施日: 2026-05-17
対象: `docs/MANUAL_TEST_CHECKLIST.md` 準拠
実施方法: **コード読解による静的確認のみ**（実ブラウザ操作なし）

---

## 判定サマリ

- 総評: **主要ロジックは実装済み**
- ブラウザ操作が必要な項目: **ブラウザ未実施**
- 指定重点観点（5件）:
  - 順位繰り下げ: **OK（コード上確認）**
  - 区切りなし自然文パース: **OK（コード上確認）**
  - サンプルデータ投入: **OK（コード上確認）**
  - localStorageバリデーション: **OK（コード上確認）**
  - タグ重複除去: **OK（コード上確認）**

---

## チェックリスト結果

### 0. 事前準備
- アプリ起動確認: **ブラウザ未実施**
- Consoleエラー確認: **ブラウザ未実施**
- localStorage初期化確認: **ブラウザ未実施**

### 1. 入力パース（基本）
- 正常入力（メモあり）: **ブラウザ未実施**
- 正常入力（メモなし）: **ブラウザ未実施**
- 順位表記ゆらぎ: **OK（`parseRank` が `[1-3]` 抽出）**
- 不正フォーマット: **OK（`parseSlashInput`/`parseNaturalInput` 失敗時 `null`）**
- 区切りなし自然文入力: **OK（`parseNaturalInput` 実装あり）**

### 2. タグ絞り込み
- 部分一致検索（タグ/店名/メモ）: **OK（`filtered` で includes）**
- タグチップ絞り込み: **OK（`setFilterText(tag)`）**
- クリア: **OK（`setFilterText('')`）**
- 表示確認: **ブラウザ未実施**

### 3. Google Mapsリンク
- URL生成ロジック: **OK（`buildMapsQuery` で `maps/search` + query encode）**
- 新規タブ遷移の実挙動: **ブラウザ未実施**

### 4. localStorage / サンプルデータ投入
- 永続化保存ロジック: **OK（`useEffect` で `localStorage.setItem`）**
- 読み込みロジック: **OK（`loadItems`）**
- サンプルデータ投入: **OK（`addSampleData` 実装、UIボタンあり）**
- 不正JSON耐性: **OK（`try/catch` で `[]`）**
- 不正要素バリデーション: **OK（`toValidItem` で型/必須値/rank/tags検証）**
- 実保存/復元動作: **ブラウザ未実施**

### 5. 編集/削除
- 編集開始: **OK（`startEdit`）**
- 編集保存: **OK（`saveEdit` + `rebalanceTheme`）**
- 編集キャンセル: **OK（`editingId` と `editingInput` をクリア）**
- 削除: **OK（`removeItem`）**
- 実画面反映確認: **ブラウザ未実施**

### 6. 順位繰り下げ（重点）
- 同一テーマ判定: **OK（`themeKeyFromTags`）**
- 繰り下げ処理: **OK（`rebalanceTheme`）**
  - 対象テーマ内で挿入位置を rank で決定
  - 上位3件に切り詰め
  - rank を 1..3 に再採番
- 4位相当の扱い: **OK（`slice(0,3)` で除外）**
- 再読み込み維持（理論）: **OK（保存/読込あり）**
- 実動作確認: **ブラウザ未実施**

---

## コード上で確認した改善反映ポイント

1. **タグ重複除去**
   - `normalizeTags()` で `Set` による一意化
   - `parseSlashInput` / `parseNaturalInput` / `parseQuickInput` で適用

2. **自然文パース対応**
   - `parseNaturalInput()`
   - 地域抽出: `pickRegionTag`
   - 料理系タグ抽出: `pickFoodTags`
   - 店名抽出: `pickName`
   - メモ抽出: `pickMemo`

3. **localStorageバリデーション強化**
   - `toValidItem()` で要素単位検証
   - 不正データは読み飛ばし

---

## JSONインポート/エクスポート検証テンプレ（追記）

> 次回手動QAでそのまま埋める用（fail-closed要件の確認を優先）

- 実施日:
- 実施者:
- 対象ビルド/コミット:

### ケースA: エクスポート
- 手順: データ1件以上で `JSONエクスポート`
- 結果: [ ] OK / [ ] NG
- メモ:

### ケースB: 正常インポート
- 手順: エクスポートJSONをそのままインポート
- 結果: [ ] OK / [ ] NG
- メモ:

### ケースC: 配列以外JSON
- 手順: `{ "foo": 1 }` をインポート
- 結果: [ ] OK / [ ] NG
- 判定基準: 既存データ保持 + エラー表示
- メモ:

### ケースD: 不正要素混在（fail-closed）
- 手順: 配列内に `id欠損` or `rank:4` or `tags:[]` を1件混在
- 結果: [ ] OK / [ ] NG
- 判定基準: 全体反映しない + 既存データ保持
- メモ:

### ケースE: 壊れたJSON
- 手順: パース不能なJSONをインポート
- 結果: [ ] OK / [ ] NG
- 判定基準: 既存データ保持 + エラー表示
- メモ:

## 残課題（今回未実施）

- 実ブラウザでの画面/導線確認（UI表示、クリック遷移、再読み込み反映）
- 実データを用いた境界ケース（自然文の抽出誤判定率など）の体感確認

---

## 結論

静的確認の範囲では、チェックリスト重点項目（順位繰り下げ・自然文パース・サンプル投入・localStorageバリデーション・タグ重複除去）は**実装済み**。
最終合格判定にはブラウザでの手動実行が必要（本結果では未実施として明記）。
