# top3-favorites QA結果（静的確認）

実施日: 2026-05-18
対象: `docs/MANUAL_TEST_CHECKLIST.md` と現行 `src/App.tsx`
実施方法: コード読解 + 静的QA契約テスト

---

## 判定サマリ

- 総評: 現行UIは `タグ / 場所 / 順位 / 店舗名 / メモ` の構造化フォームで登録し、`/api/items` 経由で保存・取得する。
- ブラウザ操作が必要な項目: 画面表示、クリック、新規タブ、再読み込み後の実挙動。
- 重点観点:
  - 順位繰り下げ: OK（`rankItems` と保存後の一覧更新で確認）
  - 必須入力バリデーション: OK（タグ・店舗名の空入力を拒否）
  - サンプルデータ投入: OK（`addSamples` が `/api/items` にPOST）
  - `/api/items` 永続化: OK（初回取得・追加・編集・削除・置換インポートがAPI経由）
  - `/api/items` 取得失敗耐性: E2EでOK（初回503、再読み込み、復旧表示を `tests/api-load-retry.e2e.spec.ts` で確認）
  - タグ一覧重複除去: OK（API返却後に `Set` で一意化）

---

## チェックリスト結果

### 0. 事前準備
- アプリ起動確認: E2Eで継続確認（`tests/startup-console-health.e2e.spec.ts`）
- Consoleエラー確認: E2Eで継続確認（初期表示時の `console.error` / `pageerror` 不在）
- APIデータ初期化確認: ブラウザ未実施

### 1. 構造化フォーム入力
- 正常入力（メモあり）: ブラウザ未実施
- 正常入力（メモなし）: ブラウザ未実施
- 順位選択: OK（draft の rank を保存）
- 必須入力不足: OK（タグ・店舗名が空なら保存しない）

### 2. タグ絞り込み
- 部分一致検索（タグ/場所/店名/メモ）: OK（`filteredGroups` で includes）
- タグチップ絞り込み: OK（`selectTag`）
- クリア: OK（`clearSelectedTag`）
- 表示確認: ブラウザ未実施

### 3. Google Mapsリンク
- URL生成ロジック: OK（店名・場所・タグを query encode）
- 新規タブ遷移の実挙動: ブラウザ未実施

### 4. `/api/items` 保存 / サンプルデータ投入
- 初回読み込み: OK（`loadItems` が `/api/items` をGET）
- API取得失敗→再読み込み復旧: E2Eで継続確認（`tests/api-load-retry.e2e.spec.ts`）
- 追加保存: OK（`saveNew` が `/api/items` にPOST）
- 編集保存: OK（`saveEdit` が `/api/items` にPUT）
- 削除: OK（`removeItem` が `/api/items?id=...` にDELETE）
- サンプルデータ投入: OK（`addSamples` がサンプルをPOST）
- 実保存/復元動作: ブラウザ未実施

### 5. JSONインポート/エクスポート
- エクスポート: OK（現在の items をJSONダウンロード）
- 配列以外JSON: OK（既存データ保持 + エラー）
- 不正要素混在: OK（既存データ保持 + エラー）
- ID重複: OK（既存データ保持 + エラー）
- 正常インポート: OK（確認プレビュー後に `/api/items?mode=replace` へPOST）

### 6. 順位繰り下げ
- 同一タグ判定: OK（タグ単位のTop3）
- 繰り下げ処理: OK（指定順位に挿入し、上位3件へ切り詰め、1..3に再採番）
- 4位相当の扱い: OK（除外理由付きプレビューで確認可能）
- 実動作確認: E2Eで継続確認

---

## JSONインポート/エクスポート検証テンプレ

- 実施日:
- 実施者:
- 対象ビルド/コミット:

### ケースA: エクスポート
- 手順: データ1件以上で `JSONエクスポート`
- 結果: [ ] OK / [ ] NG
- メモ:

### ケースB: 正常インポート
- 手順: エクスポートJSONをそのままインポートし、確認プレビューで反映
- 結果: [ ] OK / [ ] NG
- メモ:

### ケースC: 配列以外JSON
- 手順: `{ "foo": 1 }` をインポート
- 結果: [ ] OK / [ ] NG
- 判定基準: 既存データ保持 + エラー表示
- メモ:

### ケースD: 不正要素混在（fail-closed）
- 手順: 配列内に `id欠損` or `rank:4` or 空タグを1件混在
- 結果: [ ] OK / [ ] NG
- 判定基準: 全体反映しない + 既存データ保持
- メモ:

### ケースE: 壊れたJSON
- 手順: パース不能なJSONをインポート
- 結果: [ ] OK / [ ] NG
- 判定基準: 既存データ保持 + エラー表示
- メモ:

## 残課題（今回未実施）

- 実機ブラウザでの画面/導線確認（UI表示、クリック遷移、再読み込み反映）
- DevTools固有表示・ブラウザ拡張起因の警告など、Playwright 監視では拾えない実機差分

---

## 結論

静的確認の範囲では、チェックリスト重点項目（順位繰り下げ、必須入力バリデーション、サンプル投入、`/api/items` 永続化、タグ重複除去）は現行実装と整合している。
最終合格判定にはブラウザでの手動実行が必要（本結果では未実施として明記）。
