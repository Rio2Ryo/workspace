# Cloudflare 移行 QA テストケース

**担当**: 白 (Shiro)  
**作成日**: 2026-03-12  
**対象**: 分身 AI (Bunshin AI) Cloudflare 移行後  
**本番 URL**: （未設定）  
**テストアカウント**: （未設定）

---

## 🔵 正常系

### 認証・ログイン
- [ ] 未ログイン状態で保護ページにアクセスしたとき、ログイン画面へリダイレクトされること
- [ ] メール/パスワードでログイン成功し、ダッシュボードへ遷移すること
- [ ] ログイン後にブラウザをリロードしてもセッションが維持され、再ログイン不要であること
- [ ] ログアウトボタン押下でログイン画面へ遷移し、ブラウザバックで元ページに戻れないこと
- [ ] 別タブでログイン操作後、元タブでも認証状態が同期されること（複数タブ同期）
- [ ] 別タブでログアウト操作後、元タブでもログイン状態が解除されること

### API 一覧・取得
- [ ] ダッシュボード開いたとき、API キー一覧が正常に取得・表示されること
- [ ] 一覧取得中にローディングスピナーが表示されること
- [ ] 一覧取得後にリロードしても、同じデータが表示されること
- [ ] 一覧画面で 100 件以上データがあっても表示崩れや極端な遅延がないこと

### API 作成
- [ ] 新規作成フォームに入力し送信ボタン押下で、成功メッセージが表示されること
- [ ] API 作成後、一覧に新規アイテムが即時反映されること
- [ ] 作成中にローディング表示がされ、完了後ボタンが活性に戻ること
- [ ] Workers 環境で作成したデータが即時に一覧へ反映されること

### API 更新
- [ ] 編集ボタン押下で編集フォームが開くこと
- [ ] 名前変更して保存ボタン押下で、変更が一覧に反映されること
- [ ] 更新後にリロードしても、変更内容が保持されていること

### API 削除
- [ ] 削除ボタン押下で確認ダイアログが表示されること
- [ ] 削除確認後、一覧からアイテムが削除されること
- [ ] 削除後にリロードしても、削除されたアイテムが表示されないこと

### 外部連携
- [ ] Webhook 設定後テスト送信で、相手先で受信が確認できること
- [ ] 外部 API からのコールバックをアプリ内で適切に処理できること

---

## 🔴 異常系

### 認証エラー
- [ ] 間違ったパスワードでログイン試行したとき、ログイン失敗エラーが表示されること
- [ ] 有効期限切れトークンでアクセスしたとき、自動でログイン画面へリダイレクトされること
- [ ] 未認証で API エンドポイントに直接アクセスしたとき、401 エラーが返されること
- [ ] 5 回連続ログイン失敗したとき、アカウントロックメッセージが表示されること

### API エラー表示
- [ ] 存在しない ID にアクセスしたとき、404 エラーページが表示されること
- [ ] 不正なリクエストボディを送信したとき、400 エラーと具体的内容が表示されること
- [ ] 認可不足のアクセスをしたとき、403 エラーと権限メッセージが表示されること
- [ ] サーバーエラーが発生したとき、500 エラーページとサポート連絡先が表示されること
- [ ] レートリミット超過したとき、429 エラーとリトライ可能時間が表示されること

### 失敗時の UX
- [ ] API 失敗時にエラーメッセージが日本語で表示されること
- [ ] 失敗時にリトライボタンが表示されること
- [ ] リトライボタン押下で再試行が実行されること
- [ ] 連続失敗時にサポート連絡先へのリンクが表示されること

### Webhook 失敗
- [ ] Webhook 配信失敗時にエラーログに記録されること
- [ ] Webhook 失敗時にアプリ内で失敗状態が表示されること
- [ ] 配信再試行を手動/自動で実行できること

### ネットワーク障害
- [ ] オフライン状態のとき、ネットワークエラーメッセージが表示されること
- [ ] タイムアウト発生時にタイムアウトエラーが表示されること
- [ ] 通信再開後に自動でデータが再取得されること

---

## 🟡 環境差分・移行特有

### セッション・同期
- [ ] 別タブでログイン後、元タブをリロードせずとも状態が更新されること
- [ ] セッションタイムアウト時に全タブで同時にログイン画面へ遷移すること
- [ ] 複数タブで同時編集したとき、競合が適切に処理されること

### CORS・セキュリティ
- [ ] 許可されたオリジンからのアクセスが正常に処理されること
- [ ] 未許可オリジンからのアクセスが CORS エラーでブロックされること
- [ ] プリフライトリクエストが適切に処理されること
- [ ] 開発環境と本番環境で CORS 設定に差分がないこと

### 環境変数
- [ ] 環境変数不足時に適切なエラーメッセージが表示されること
- [ ] Wrangler secrets が本番環境で正しく設定されていること
- [ ] 開発環境と本番環境で環境変数に差分がないこと

### Pages/Workers 挙動
- [ ] Pages デプロイ後に即座に反映されること
- [ ] Workers エッジでのレイテンシが許容範囲内であること
- [ ] ビルド後バンドルサイズが許容範囲内であること

### パフォーマンス
- [ ] 初期表示 FCP が 3 秒以内であること
- [ ] 初期表示 LCP が 2.5 秒以内であること
- [ ] API レスポンス P95 が 500ms 以内であること
- [ ] 大量データスクロール時にカクつきがないこと

### コンソールエラー
- [ ] 本番 URL を開いたとき console.error が出力されていないこと
- [ ] 全操作実施時に console.warn が許容範囲内であること
- [ ] DevTools ネットワークタブで失敗リクエストがないこと

### ブラウザ・デバイス
- [ ] Chrome 最新版で全機能が動作すること
- [ ] Safari 最新版（Mac）で全機能が動作すること
- [ ] Safari（iOS）で全機能が動作すること
- [ ] Firefox 最新版で全機能が動作すること
- [ ] スマホ表示でレイアウト崩れがないこと
- [ ] タブレット表示でレイアウト崩れがないこと

### 監視・ログ
- [ ] エラー発生時に Cloudflare Logs に記録されること
- [ ] 各リクエストにトレース ID が付与されること
- [ ] 閾値超過時に監視アラートが通知されること

### データ整合性
- [ ] 既存データの移行漏れがないこと
- [ ] 書き込み後の読み取りでデータが整合していること
- [ ] 削除・更新操作が正しく反映されること

---

## 📋 優先順位（移行直後に事故りやすい順）

### P0（必須・リリースブロック）
**認証/セッション維持**
- 未ログイン状態で保護ページにアクセスしたとき、ログイン画面へリダイレクトされること
- メール/パスワードでログイン成功し、ダッシュボードへ遷移すること
- ログイン後にブラウザをリロードしてもセッションが維持され、再ログイン不要であること
- 別タブでログイン操作後、元タブでも認証状態が同期されること（複数タブ同期）
- 別タブでログアウト操作後、元タブでもログイン状態が解除されること
- 有効期限切れトークンでアクセスしたとき、自動でログイン画面へリダイレクトされること

**API 疎通（一覧・作成・更新・削除）**
- ダッシュボード開いたとき、API キー一覧が正常に取得・表示されること
- 新規作成フォームに入力し送信ボタン押下で、成功メッセージが表示されること
- API 作成後、一覧に新規アイテムが即時反映されること
- 編集ボタン押下で編集フォームが開くこと
- 名前変更して保存ボタン押下で、変更が一覧に反映されること
- 削除ボタン押下で確認ダイアログが表示されること
- 削除確認後、一覧からアイテムが削除されること
- Workers 環境で作成したデータが即時に一覧へ反映されること

**CORS**
- 許可されたオリジンからのアクセスが正常に処理されること
- 未許可オリジンからのアクセスが CORS エラーでブロックされること
- 開発環境と本番環境で CORS 設定に差分がないこと

**env 不足時の致命的エラー**
- 環境変数不足時に適切なエラーメッセージが表示されること
- Wrangler secrets が本番環境で正しく設定されていること
- 開発環境と本番環境で環境変数に差分がないこと

**本番 URL での console error**
- 本番 URL を開いたとき console.error が出力されていないこと
- DevTools ネットワークタブで失敗リクエストがないこと

### P1（重要・早期対応）
**失敗時メッセージ**
- 間違ったパスワードでログイン試行したとき、ログイン失敗エラーが表示されること
- 存在しない ID にアクセスしたとき、404 エラーページが表示されること
- サーバーエラーが発生したとき、500 エラーページとサポート連絡先が表示されること
- API 失敗時にエラーメッセージが日本語で表示されること

**リトライ導線**
- 失敗時にリトライボタンが表示されること
- リトライボタン押下で再試行が実行されること
- 連続失敗時にサポート連絡先へのリンクが表示されること
- 配信再試行を手動/自動で実行できること

**データ整合性**
- 一覧取得後にリロードしても、同じデータが表示されること
- 更新後にリロードしても、変更内容が保持されていること
- 削除後にリロードしても、削除されたアイテムが表示されないこと
- 既存データの移行漏れがないこと
- 書き込み後の読み取りでデータが整合していること

**webhook/外部連携**
- Webhook 設定後テスト送信で、相手先で受信が確認できること
- 外部 API からのコールバックをアプリ内で適切に処理できること
- Webhook 配信失敗時にエラーログに記録されること
- Webhook 失敗時にアプリ内で失敗状態が表示されること

### P2（あれば良い）
- 一覧画面で 100 件以上データがあっても表示崩れや極端な遅延がないこと
- 作成中にローディング表示がされ、完了後ボタンが活性に戻ること
- 5 回連続ログイン失敗したとき、アカウントロックメッセージが表示されること
- 不正なリクエストボディを送信したとき、400 エラーと具体的内容が表示されること
- 認可不足のアクセスをしたとき、403 エラーと権限メッセージが表示されること
- レートリミット超過したとき、429 エラーとリトライ可能時間が表示されること
- オフライン状態のとき、ネットワークエラーメッセージが表示されること
- タイムアウト発生時にタイムアウトエラーが表示されること
- 通信再開後に自動でデータが再取得されること
- セッションタイムアウト時に全タブで同時にログイン画面へ遷移すること
- 複数タブで同時編集したとき、競合が適切に処理されること
- プリフライトリクエストが適切に処理されること
- Pages デプロイ後に即座に反映されること
- Workers エッジでのレイテンシが許容範囲内であること
- ビルド後バンドルサイズが許容範囲内であること
- 初期表示 FCP が 3 秒以内であること
- 初期表示 LCP が 2.5 秒以内であること
- API レスポンス P95 が 500ms 以内であること
- 大量データスクロール時にカクつきがないこと
- 全操作実施時に console.warn が許容範囲内であること
- Chrome/Safari/Firefox 最新版で全機能が動作すること
- スマホ/タブレット表示でレイアウト崩れがないこと
- エラー発生時に Cloudflare Logs に記録されること
- 各リクエストにトレース ID が付与されること
- 閾値超過時に監視アラートが通知されること

---

## 📝 実行メモ

| 日付 | テスト項目 | 結果 | 備考 |
|------|-----------|------|------|
| | | | |

- 各チェック項目には**スクリーンショット/ログ**を添付
- 不具合発見時は**再現手順**を明記
- 環境変数名は**機密保持**のためマスキング
