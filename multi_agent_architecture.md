# マルチエージェント基盤 - 詳細設計

## 目的
- 白（親機）をオーケストレーターとして、複数のサブエージェントを管理する24時間運用体制
- 柔軟なタスク割り当て・負荷分散・フェイルオーバーを実現

## アーキテクチャ

### コンポーネント

```
┌─────────────────────────────────────────────────────────┐
│                    OpenClaw Gateway                      │
│                   (Orchestration Core)                   │
└──────────────┬──────────────────────────────────────────┘
               │
       ┌───────┴───────┐
       │               │
┌──────▼──────┐  ┌────▼─────┐  ┌──────────┐
│ 白 (Shiro)  │  │  黒    │  │  サブ    │
│ 親機・司令塔 │  │ 機動支援 │  │  エージェント  │
└─────────────┘  └──────────┘  └──────────┘
       │               │               │
       └───────────────┴───────────────┘
                       │
           ┌───────────┼───────────┐
           │           │           │
      ┌────▼───┐  ┌────▼────┐  ┌──▼──────┐
      │ X運用  │  │ QA    │  │ 監視     │
      │エージェント│  │エージェント│  │エージェント│
      └────────┘  └─────────┘  └─────────┘
```

### セッション管理

**親機（白）の責務:**
- メインセッションとして常時稼働
- サブエージェントの起動・監視・終了
- タスクの割り当てと結果の統合
- 異常検知とフェイルオーバー

**サブエージェントのライフサイクル:**
```
起動 (sessions_spawn) → タスク実行 → 報告 → 終了 (cleanup:delete)
```

## API仕様

### サブエージェント起動
```javascript
sessions_spawn({
  agentId: "qa-sub-agent",  // エージェントID
  task: "Second BrainのQA実行",
  label: "qa-2026-02-18",  // ラベル（識別用）
  cleanup: "delete",        // 完了後削除
  timeoutSeconds: 1800      // 30分タイムアウト
});
```

### セッション一覧・監視
```javascript
sessions_list({
  kinds: ["sub-agent"],     // サブエージェントのみ
  activeMinutes: 60         // 直近60分のアクティブセッション
});
```

### セッションへのメッセージ送信
```javascript
sessions_send({
  sessionKey: "qa-2026-02-18",
  message: "優先度P1: モバイルUIのテストを追加で実行"
});
```

## タスク割り当てロジック

### スケジューラ（Cron Jobs）

**例: 定期QA実行**
```javascript
cron({
  action: "add",
  job: {
    name: "daily-qa",
    schedule: {
      kind: "cron",
      expr: "0 9 * * *",  // 毎日9時
      tz: "Asia/Tokyo"
    },
    payload: {
      kind: "agentTurn",
      message: "Second Brainの定期QA実行"
    },
    sessionTarget: "isolated"
  }
});
```

## 監視・アラート

### ヘルスチェック
- サブエージェントの応答性監視
- タイムアウト検知
- エラー回数の集計

### アラート通知
- **P0**: サブエージェントの死亡・応答なし → 即座に再起動
- **P1**: 連続エラー3回 → ログ確認・調査
- **P2**: 定期タスクの遅延 → スケジュール再考

## セキュリティ

### アクセス制御
- `agents_list` で起動可能なエージェントIDの確認
- 許可されたエージェントのみ起動可能

### データ分離
- 親機とサブエージェントのデータ分離
- 必要な情報のみ `sessions_send` で共有

## 次のステップ
1. OpenClawのセッション管理機能の詳細確認
2. `agents_list` で利用可能なエージェントIDの確認
3. プロトタイプの実装（白 → サブエージェント起動 → 報告）
