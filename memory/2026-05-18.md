# 2026-05-18

- Yakonさんから `安全な一手` 系の表現を再度禁止され、「一旦何でもやって」と指示。以後、作業報告やkeeper投入文ではその表現を使わず、原因分析→実装→検証→結果報告に直結させる。`ops/tmux-agent-keeper.sh` の再開/ミッション文から該当表現を除去。
- top3-goose: Top3 import正規化の不整合を継続対応。`src/App.tsx` と E2E 3件を整備し、`pnpm test:e2e --reporter=line` 44 passed、`pnpm build` 成功を確認。

## Keeper停止再発インシデント — 2026-05-18 14:16 JST
- 何が起きたか: Yakonさんから「全セッション止まってる気がする」「これ何度め？いわれないとあかんの？」と再指摘。keeperが完了後待機/承認待ち/圧縮interruptを十分に再投入できず、管理サマリーもノイズになった。
- なぜ起きたか: (1) 承認待ちを報告だけして自動継続しない分岐が残っていた、(2) management digestをDiscordへ定期投稿してノイズ化、(3) cooldownが再投入を妨げ、止まり検知が行動に直結していなかった。
- 次回どう防ぐか: keeperを `KEEPER_ASSUME_APPROVED=1` で起動し承認/入力promptへEnter送信、`KEEPER_DIGEST_TO_DISCORD=0` でサマリーはdashboardのみ、interval/cooldownを短縮。人間に残すのは秘密値・本番設定変更・削除・課金増など実リスクのみ。
- 反映: `ops/tmux-agent-keeper.sh` を修正し、agent-keeperを `KEEPER_INTERVAL_SECONDS=90 KEEPER_COOLDOWN_SECONDS=300 KEEPER_AUTO_ACTION_COOLDOWN_SECONDS=120 KEEPER_ASSUME_APPROVED=1 KEEPER_DIGEST_TO_DISCORD=0` で再起動。

## 再発: 汎用keeper文言を非コード作業へ誤適用
- 何が起きたか: `resume-claude`（職務経歴書作成・人間素材待ち）に対して、keeperが「原因分析→仮説→実装→テスト→結果報告」というコード作業用の汎用再投入通知を #白 に送った。Yakonさんから「同じことばかり。考えてない」と指摘。
- なぜ起きたか: keeperが全tmuxセッションを同じ型（実装エージェント）として扱い、非コード/人間入力待ちセッションの状態モデルを持っていなかった。また `send_action_notice` が抽象的な再投入文をDiscordへ投稿していた。
- 次回どう防ぐか: 汎用「再投入」通知をDiscordへ出さない。`resume-claude` は人間素材待ちを正常状態として扱い、自律promptも送らない。作業種別ごとの状態モデルを入れる。
- 反映: `ops/tmux-agent-keeper.sh` で `send_action_notice` をログのみへ変更、`resume-claude` のthread mapping追加、idle時は `waiting for user resume素材; no autonomous prompt` としてskip。
# 2026-05-18

- Yakonさんから `安全な一手` 系の表現を再度禁止され、「一旦何でもやって」と指示。以後、作業報告やkeeper投入文ではその表現を使わず、原因分析→実装→検証→結果報告に直結させる。`ops/tmux-agent-keeper.sh` の再開/ミッション文から該当表現を除去。
- top3-goose: Top3 import正規化の不整合を継続対応。`src/App.tsx` と E2E 3件を整備し、`pnpm test:e2e --reporter=line` 44 passed、`pnpm build` 成功を確認。

## Keeper停止再発インシデント — 2026-05-18 14:16 JST
- 何が起きたか: Yakonさんから「全セッション止まってる気がする」「これ何度め？いわれないとあかんの？」と再指摘。keeperが完了後待機/承認待ち/圧縮interruptを十分に再投入できず、管理サマリーもノイズになった。
- なぜ起きたか: (1) 承認待ちを報告だけして自動継続しない分岐が残っていた、(2) management digestをDiscordへ定期投稿してノイズ化、(3) cooldownが再投入を妨げ、止まり検知が行動に直結していなかった。
- 次回どう防ぐか: keeperを `KEEPER_ASSUME_APPROVED=1` で起動し承認/入力promptへEnter送信、`KEEPER_DIGEST_TO_DISCORD=0` でサマリーはdashboardのみ、interval/cooldownを短縮。人間に残すのは秘密値・本番設定変更・削除・課金増など実リスクのみ。
- 反映: `ops/tmux-agent-keeper.sh` を修正し、agent-keeperを `KEEPER_INTERVAL_SECONDS=90 KEEPER_COOLDOWN_SECONDS=300 KEEPER_AUTO_ACTION_COOLDOWN_SECONDS=120 KEEPER_ASSUME_APPROVED=1 KEEPER_DIGEST_TO_DISCORD=0` で再起動。

## 再発: 汎用keeper文言を非コード作業へ誤適用
- 何が起きたか: `resume-claude`（職務経歴書作成・人間素材待ち）に対して、keeperが「原因分析→仮説→実装→テスト→結果報告」というコード作業用の汎用再投入通知を #白 に送った。Yakonさんから「同じことばかり。考えてない」と指摘。
- なぜ起きたか: keeperが全tmuxセッションを同じ型（実装エージェント）として扱い、非コード/人間入力待ちセッションの状態モデルを持っていなかった。また `send_action_notice` が抽象的な再投入文をDiscordへ投稿していた。
- 次回どう防ぐか: 汎用「再投入」通知をDiscordへ出さない。`resume-claude` は人間素材待ちを正常状態として扱い、自律promptも送らない。作業種別ごとの状態モデルを入れる。
- 反映: `ops/tmux-agent-keeper.sh` で `send_action_notice` をログのみへ変更、`resume-claude` のthread mapping追加、idle時は `waiting for user resume素材; no autonomous prompt` としてskip。

## 自律運転スキル改善指示 — 2026-05-18 17:14 JST
- Yakonさんから「その次進めることって自律的に動かないよね。自律的に動くスキルを探してインストールして」と指示。
- 既存の `agent-autonomy-kit` は導入済みだが、実態はkeeperのIDLE再投入に寄っており、`次に進めること` が抽象的でセッション自身の自律判断に落ちていないのが問題。
- 次にやること: ClawHubで自律運転/チーム運用/タスクキュー系スキルを検索し、より具体的な `DISCOVER → TRIAGE → READY → EXECUTE → FEEDBACK` 型またはセッション別キュー型のスキルをインストール・適用する。導入後は `tasks/QUEUE.md` と `ops/autonomy-heartbeat.mjs` / keeperの状態モデルへ反映し、抽象promptではなく具体Readyタスクを渡す。
- 制約: Discordへ汎用再投入報告を出さない。`resume-claude` は人間素材待ち正常。`threads-watcher` は異常時のみ。通常のcommit/push/deployは進め、秘密値・本番設定変更・削除・課金増・公開範囲拡大だけ確認。
