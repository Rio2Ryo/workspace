# 2026-04-28

## Mother Vegetable 本番画像差し替え対応
- Discord `#mother-vegetable` / thread `mother-vegetable 引き継ぎ` で、aji の画像設定作業が長時間止まっていたため、Ryo/やまざき側のOKを受けて白が引き取り。
- 対象リポジトリ: `/Users/umi/.openclaw/workspace/projects/mother-vegetable`。
- 対象: サンケアジェル、ハンドクリーム、味噌の3件の画像差し替え。
- 画像差し替えコミットとして `a7a39de` を作成済みと報告。
- 本番反映には `vercel deploy --prod` が必要で、やまざきから「ＯＫ」を受領。
- Vercel CLI 直デプロイは build 成功後 `Deploying outputs...` の後に Error / 固まる挙動があり、完了扱いにしていない。原因切り分け中。
- CLI直デプロイが不安定なため、ローカル変更を残さず GitHub push による通常の本番連携ルートも動かす方針を表明。
- 本番反映完了までは、画像反映を完了扱いにしないこと。

## Mother Vegetable 既存方針・注意
- 本番サイト `https://mothervegetable.co.jp` の検証は、実注文/実決済を踏まずに checkout 遷移と API 存在確認までで止める。
- `/api/checkout` の本番POST検証は原則不要。追加検証は preview/dev 側で行う。
- 本番Stripeの実決済や実注文作成は禁止。必要でも Stripe Checkoutページ表示 or `/api/checkout` の `200+URL返却` まで。
- Discordで aji メンションは `<@1479140779313074377>`、Yakon は `<@797097185098858508>` を使う。
- `rg` は環境になく `zsh:1: command not found: rg` が出たため、この環境では `grep` / `git grep` を使う。

## Mother Vegetable 残タスク
- デプロイ完了後に本番で以下を再確認:
  - サンケアジェル、ハンドクリーム、味噌のトップ/詳細画像が反映されていること。
  - `/ja/product` → `/ja/products` redirect。
  - 商品詳細HTML/構造化データに `USD` 残存なし、`JPY` 統一。
  - トップの「すべてのニュースを見る」リンクが `/ja/news`。
  - checkout 直前までの導線。
- 既知残課題: E2E テストの mock 依存（checkout/purchase-flow が実 API を通していない）、Facebook OAuth / AI チャットボット（env 未設定）。
