- tmux Web View: Yakonさん依頼で Sora VPS の root tmux sessions を表示対象に追加。Sora側で `/root/tmux-feed-server.mjs` を `tmux-feed`、Cloudflare Tunnel を `tmux-feed-tunnel` として起動。表示対象: `sora-hermes`, `sora-codex`, `sora-claude`, `sora-goose`, `sora-openclaw`, `sora-grok`。Vercel `tmux-web-view` production env/deploy 反映済み、`/api/sessions` と `sora-hermes` snapshot 取得OK。
