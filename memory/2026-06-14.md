# 2026-06-14

## 23:25 JST — 既存cron全停止とShiro Loop v2着手
- Yakonより、Loop Engineering方針で進めることと「いまやってるcronはすべてやめて」と指示。
- 対応: OpenClaw cronの有効ジョブ4件をすべてdisabled化。有効cronは0件で確認済み。停止対象は `shiro-loop-tick`、`tmux-autopilot: stalled session nudge`、`朝のブリーフィング - シロからの元気な挨拶`、`shiro-loop-digest`。
- 実装: `ops/shiro-loop-v2.mjs` と `ops/shiro-loop-v2.md` を追加。cron再作成ではなく、手動実行で `state/shiro-loop-v2/loops.json` と `state/shiro-loop-v2/next-actions.md` を生成する方式に変更。各sessionに Goal / Loop / Pass Criteria / Autonomous Allowed / Confirmation Required / Verifier 状態を持たせる。
- 次回基準: 新しい定期実行はYakon承認まで作らない。まずスレッド単位の検証可能ゴールと独立Verifierを整備し、YakonにはYes/Noで答えられる承認パケットだけを出す。

## 02:06 JST — 承認待ちと「Yakonさんやることなし」の矛盾修正
- Yakonより「承認待ちが大量にあるのに俺のやることないの？」と指摘。
- 原因: `shiro-loop-digest` が「YakonさんがYes/Noで答えられる具体文になっていないもの」を白の整理待ちとして扱う一方、同じ項目を `permission-wait` / 承認待ちとして表示していた。さらに `needsPacket` と `needsRefine` が重複し、14セッションが26件表示になっていた。
- 修正: `ops/shiro-loop-digest.mjs` を更新し、`permission-wait` / `consultRequired` / `external-action` / `done-signoff` が存在する限り「Yakonさんがやることあり（白が具体化中）」と表示するよう変更。白の作業件数はセッション単位で重複排除。
- 次回基準: 「承認待ち」「判断待ち」「permission-wait」が1件でもある場合、Yakon側の作業/判断は「なし」と言わない。未精製なら「あり（白が具体化中）」と表現する。

## 09:55 JST — RAKUI PDF読み込み未完了の監査漏れ
- 何が起きたか: やまざきさんから「昨日の夜『pdf読み込みできるまで修正を続けて』と指示したのに、まだ読み込めていない」と指摘。画面ではPDFは取り込まれていたが、RAKUI項目への自動マッピングが0件だった。
- なぜ起きたか: Sora/Codex側はPDF修正commit、単体テスト、buildを完了扱いにしていたが、実際の完了条件である「画面上でPDFから案件項目が0件でなく読めること」を監査していなかった。白も完了条件の検証を追い切れていなかった。
- 対応: `real-estate-app` でマイソク/重要事項説明書系PDFのOCR崩れに対応するため、住所・価格・面積・賃料・築年月のゆるい抽出を追加。commit `f11765f`、E2E期待値更新 `d6b3568` を `Rio2Ryo/yakon-rakui-app` mainへpush。Cloudflare Pages `https://7add150c.yakon-rakui-app.pages.dev` へdeploy。`npm test` 22 files / 123 tests PASS、`npm run build` PASS、`npm run e2e` 10 passed。
- 次回どう防ぐか: 「できるまで修正」は、実装commitでは完了にしない。該当画面の実操作またはE2Eで、ユーザーが見ている失敗条件が解消したことを確認してから完了報告する。PDF/Excel取込系は最低条件として「対象ファイルを入れて候補件数が0でない」「合計自動入力件数が表示される」を確認する。

## 10:15 JST — RAKUI最新URLでもPDF 0件が継続
- 何が起きたか: やまざきさんが最新と案内した `https://7add150c.yakon-rakui-app.pages.dev` でも、`01_property-flyer_maisoku.pdf` / `02_disclosure-report...pdf` が「Document text was readable but no known RAKUI intake labels were found」で0件のままだと指摘。
- なぜ起きたか: 前回修正のE2EはきれいなテキストPDFと同一行内のOCR崩れを中心に見ており、実PDFで起こる「日本語ラベルが1文字ずつ別行に分断される」ケースを再現できていなかった。`compactDocumentText()` が改行結合後に再正規化していなかったため、`物 件 所 在 地` 相当が `物件所在地` に戻らず、既知ラベル検出が0件になった。
- 対応: `Rio2Ryo/yakon-rakui-app` で `compactDocumentText()` の改行結合後に再度 `normalizeExtractedText()` を通す修正を追加。分断OCRラベルの回帰テストを追加。commit `2bbfe2d`, `201f625`, `6321d33` をmainへpushし、Cloudflare Pages `https://c7b6775a.yakon-rakui-app.pages.dev` へdeploy。公開bundleに修正反映を確認。`documentExtractor.test.ts` 8 tests PASS、`npm run build` PASS。
- 残リスク: ローカルcloneでは `data/final/master_file.xlsx` / `out_file.xlsx` が無く全体testのExcel fixture系4件が失敗。Playwrightは `/snap/bin/chromium` 不在でE2E実行不可。実PDFそのものはDiscord添付画像のみで入手できていないため、やまざきさんの2PDFでの実投入確認が必要。
- 次回どう防ぐか: PDF取込の「修正済み」報告は、実PDFまたは同等に壊したfixtureで0件ではないことを確認してから出す。Claudeで読める品質を要求された場合は、静的Pages内の正規表現/OCRだけで完了扱いにせず、Cloudflare Worker等でClaude/LLM抽出フォールバックを設計対象に入れる。
