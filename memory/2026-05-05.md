
## 2026-05-05 pre-compaction flush
- tmux Web View project at `/Users/umi/.hermes/workspace/tmux-web-view` was iteratively configured for Ryo:
  - Vercel `TMUX_FEED_URL` was pointed at a temporary Cloudflare tunnel to Mac mini feed API.
  - Web UI now displays `hermes-jp`, `claude`, and `codex` simultaneously in responsive columns.
  - Local feed allowed sessions includes `codex` in addition to `hermes-jp` and `claude`.
  - Viewer token support was clarified; token from URL is stored in localStorage.
  - Auto-refresh UI was improved to show clear ON/OFF state and per-session captured time, with cache-busting.
  - Feed redaction was hardened for Discord mentions, long numeric IDs, and OpenAI request IDs.
- tmux session conventions established by Ryo:
  - If Ryo says “Hermesに”, “tmuxのHermes”, or similar, operate on tmux session `hermes-jp` by default.
  - If Ryo says “codexに”, operate on tmux session `codex` by default.
  - If Ryo says “claudeに”, operate on tmux session `claude` by default.
  - Ryo explicitly asked that “tmuxの” should not be necessary going forward.
- `codex` tmux session:
  - Was started and added to Web view.
  - OpenAI/Codex device login was completed after code flow; `Hello` response verified: “Hello. What should we work on?”
- `claude` tmux session:
  - Had a stuck `Could not process image` 400 error due to an unprocessable image remaining in conversation context.
  - Fixed by running `/clear`, sending a recovery check, confirming “復旧OK”, and clearing tmux pane history so Web view no longer shows stale error.
- Hermes configuration:
  - Hermes config `/Users/umi/.hermes/config.yaml` was changed from Gemini default to `provider: openai-codex`, `model: gpt-5.5` after OpenAI device login completed.
  - `hermes-jp` tmux session was restarted.
  - Hermes reported model/provider as `gpt-5.5 / openai-codex`.
- Image-generation workflow notes:
  - Ryo asked codex to create a 9:16 single image based on `kataomoi.org`; first result was too abstract for outsiders.
  - Ryo corrected that outputs should be fetched and posted directly into Telegram, not just saved to a local path inaccessible to him.
  - Future image artifacts should be attached directly via `MEDIA:<path>` in the current chat when requested, with each artifact displayed separately if multiple.
- AI E2E QA direction:
  - Ryo wants “コード理解ベースAI E2E QA”: read source code to extract specs/logic, then generate and execute screen-level E2E test cases to verify all relevant behavior.
  - Standard workflow: source-code spec extraction with evidence → classify normal/abnormal/boundary/permission cases → browser/Playwright execution → failure classification → Playwright fixed-test candidates.
  - Created template materials under `qa-ai-e2e/`: `CODE_UNDERSTANDING_E2E_QA.md`, `codex-prompt.md`, and `README.md`.
  - Future requests like “この機能をAI E2E QAして” should default to this method.
- Codex `/goal` feature rollout for Agent Memory Platform:
  - Updated Codex CLI from `0.125.0` to `0.128.0` via npm.
  - Enabled `[features] goals = true` in `~/.codex/config.toml` with backup created.
  - Restarted tmux `codex` session and set the long-term North Star `/goal` for AI agent command center + Obsidian-style memory platform.
  - Codex goal continuation produced a Step 1 planning direction but appeared to hang while applying the planning artifact; interrupted and paused the goal.
  - Implemented Step 1/2 directly in `second-brain`: `docs/AGENT_MEMORY_PLATFORM_MVP.md`, D1 migration `0010_agent_command_center.sql`, API route `agent-command`, and route registration.
  - Verification: `pnpm -C second-brain/apps/api typecheck` passed.
  - Continued Step 3 MVP: added web page `apps/web/src/app/agents/command-center/page.tsx` and linked it from `/agents`.
  - Verification: `pnpm -C second-brain/apps/web typecheck` passed.
- Codex stop RCA and recovery:
  - First stop cause: Codex `/goal` turn idled after an `apply_patch` tool call; Codex process was alive, but tool result did not return and the turn was interrupted after ~402s.
  - Second stop cause: after upgrade to Codex CLI `0.128.0`, Node-based commands spawned under Codex (`pnpm`, `tsc`, `wrangler dev`) could remain as foreground/background child processes and make Codex keep waiting. The same commands completed normally from OpenClaw shell, so this was treated as Codex exec/runtime behavior, not a Second Brain type error.
  - Recovery: killed stuck Codex child processes, downgraded Codex CLI back to `0.125.0`, restarted tmux session `codex`, and avoided running long-lived dev servers inside Codex foreground.
  - Operational rule: for Codex tmux sessions, run long-lived servers (`next dev`, `wrangler dev`) outside Codex via OpenClaw exec/background, then ask Codex to use short `curl`/inspection commands only.
  - Second Brain MVP verification after recovery: full `pnpm -C second-brain typecheck` passed; local API `/agent-command/overview` returned agents/models/memory sources; web `/agents/command-center` returned 200 and SSR contained Agent Command Center, Shiro/Sora/Ao/Kuro, model registry, assignment advisor, and memory sources.
