# 2026-05-04

## Discord / Mother Vegetable 作業メモ
- Ryoさんから、添付画像の内容をもとに `sefs.mothervegetable.co.jp` 用のSEFS専用LPを作りたい依頼があった。背景は白ベースでクリーン・清潔感、Mother Vegetable本体LPや商品ページとは混ぜない方針。公開・DNS・Vercel本番反映は事前確認必須。
- 最初にこちらで `projects/mother-vegetable/src/app/(site)/[locale]/facility/page.tsx` や `sefs/page.tsx` を直接作ってしまったが、Ryoさんから「Shiroが作るのではなく aji に指示して作ってもらう」と訂正された。以後、依頼は aji 宛に明確にメンションして渡す必要あり。
- aji宛依頼文の要点: `sefs.mothervegetable.co.jp` 用LP、白背景・クリーン、添付画像内文言をLP本文として展開、SEFS循環型養殖システム専用LP。主な内容は「家庭や他業種の企業でも簡単に魚の養殖」「食の未来を支える持続可能な循環型システム」「太陽光で自家発電」「スーパーウッドで安心・安全」「漁協連携・稚魚仕入れ」「高速栽培LED」「未来の食料危機を解決」「ノーケミカル」「高生産効率」「簡単設置/どこでも設置」「中小企業に最適」「持続可能な未来」。
- Discordで aji へのメンションが吹き出し分割で抜ける問題をRyoさんに指摘された。今後グループチャットで他AI/人に指示する時は、同一メッセージ本文冒頭に対象メンションを確実に含めること。
- Ryoさんは aji/Codex が確認画面で頻繁に止まることを嫌がっている。運用ルールとして、低リスク作業（新規/編集、画像配置、ローカルブランチ、build/lint/tsc、スクショ、ローカルサーバ、テスト、並列エージェント編成）は自動進行。高リスク（git push、PR、main merge、Vercel本番/ドメイン/エイリアス、DNS、削除、外部送信、課金）は必ず事前確認。A/B/Cで止まる場面はデフォルト提案 C: preview先行。ただし push 自体は確認対象。
- aji側の進捗: ブランチ `feat/sefs-lp`。ローカルコミット済みでpush前。SEFS LP commit `670eda8`、商品一覧再設計 commit `ca432e2`、白カード修正 commit `5ff5615`。
- 商品一覧改善の依頼: トップページの商品一覧をAmazon検索結果のようなコンパクト一覧へ。検索はキーワード検索＋地域タグ（例: 河津町）＋属性/ストーリータグ（例: 漁協の人が作った）。商品は今後増えていく想定。
- ajiの実装内容（Phase 1/2）: `src/data/products.ts` を拡張し `producer`/`region`/`regionTags`/`storyTags`/`storyDescription` 付与、`src/data/tags.ts` 新設、`ProductGrid.tsx` と `ProductSearchFilter.tsx` 新設。`ProductsSection.tsx` を巨大カードからAmazon風2〜5列コンパクトグリッドへ置換。検索バー、地域/ストーリータグチップ、URLクエリ同期、AND絞り込み、カードタグクリックでフィルタ追加。`getHomeProductCards(locale)` 経由で `src/data/products.ts` をsingle source of truth化。ProductsSection 1180行→227行。
- ajiの検証: 型/lint/buildクリア、本番ビルド成功、191/191静的ページ。スクショでデスクトップ/モバイル/河津町フィルタを共有済み。
- 商品一覧UIは最初ダークカードだったが、白カード＋緑アクセントへ修正済み。Achieve/Confidence画像は元の `images[0]` がワードマーク画像だったため、`HOME_THUMBNAIL_OVERRIDES` で `_10005.jpg` の正規プロダクトフォトに差し替え、video撤去、全カード `<img>` 統一、フォールバック実装。
- 未処理/確認事項: push/PR/本番反映は承認待ち。`/products` 一覧ページはまだ別実装で未対応、トップと同じcompact gridに揃えるか要確認。月別タイムラインは撤去済み、必要なら「月別表示」トグルで復活可能。タグ/生産者/地域はダミーデータで、本データへ後で上書き予定。
