# 2026-05-16

## 定期キーパー

- 未完了タスク確認:
  - `second-brain/docs/AGENT_MEMORY_PLATFORM_MVP.md` に明確な次アクションあり。
  - Agent Memory Platform Step 4 は「metadata-only memory source refresh」が次の安全作業。
- 実施:
  - `agent-command` の refresh 実装を確認。
  - refresh 対象パスを棚卸し: root 5件 + `memory/YYYY-MM-DD.md` 21件 = 26件。
  - `pnpm -C second-brain/apps/api test src/routes/agent-command.test.ts` 実行。13 tests passed。
- ブロッカー:
  - ローカルD1への実書き込みは未完了。
  - `wrangler d1 migrations apply second_brain --local` がサンドボックス制約で失敗。
  - 失敗理由: `/Users/umi/Library/Preferences/.wrangler/logs/...` へのログ書き込み `EPERM` と、`127.0.0.1` listen `EPERM`。
- 次回の安全な続き:
  - sandbox外または許可されたローカル環境で `wrangler dev` / D1 local が使える状態にしてから、`POST /agent-command/memory-sources/refresh` を実行する。
  - body は `paths` のみ。本文、raw logs、token、secret は渡さない。
  - 対象は `AGENTS.md`, `MEMORY.md`, `IDENTITY.md`, `USER.md`, `HEARTBEAT.md`, `memory/*.md`。

## 定期キーパー追記

- 継続確認:
  - 前回の未完了は継続中。実DB refresh はサンドボックス内ではまだ不可。
- 追加検証:
  - `WRANGLER_LOG_PATH=tmp/wrangler-logs` と `--persist-to ../../tmp/wrangler-persist` でホーム配下ログ書き込み問題は回避できることを確認。
  - ただし Miniflare/wrangler が `127.0.0.1` listen を必要とし、現在のサンドボックスでは `EPERM` のため停止。
  - `pnpm -C second-brain/apps/api test src/routes/agent-command.test.ts` は再実行し、13 tests passed。
- 整理:
  - 次回実行用 runbook を `second-brain/docs/MEMORY_SOURCE_REFRESH_RUNBOOK.md` に作成。
