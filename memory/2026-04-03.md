## 2026-04-03
- Discord運用メモ: このチャンネルでは @Shiro-mini へのメンションがある時だけ反応する方針をユーザーから明示された。返信は Discord/OpenClaw 上で暗黙メンション扱いになることがあるため、完全な明示メンション限定にするには `requireMention` / reply-to-bot 周りの設定調整が必要。
- browser-use 運用メモ: Shiro が判断し browser-use を手足として使う CLI 操作（open/state/click/input/screenshot）であれば browser-use 用の LLM API キーは不要。browser-use 自体に自然言語タスクを任せる Agent 運用では LLM が必要。
- browser-use ローカル導入メモ: このMacでは `uvx --python 3.13 --from browser-use browser-use ...` で利用可能。Python 3.14 では `doctor` が落ちたため、現時点では Python 3.13 前提が安全。1セッションの Chrome 系メモリ感は約700MB前後で、16GB Mac なら 1〜3 セッションの検証用途は現実的。
- agent-device iOS Simulator 検証メモ: Settings / Safari で `snapshot / press / type / fill / back / screenshot` は実用ライン。日本語入力も通る。一方 `home` は効いていない/観測しづらい可能性があり、`close` はアプリ終了というより AgentDeviceRunner に落ちる挙動で注意。
- food DX browser-use 検証メモ: `https://food-dx-system.common-gifted-tokyo.workers.dev` でトップ表示、`/auth/login` 表示、デモアカウント `demo@foodflow.local / DemoFoodFlow2026!` でログイン後 OCR 画面到達、`/ocr` の未ログイン閲覧を確認。証跡スクショを workspace に保存済み。
- OpenClaw 更新メモ: 2026.3.13 から 2026.3.28 (f9b1079) へ更新したとユーザーに報告。変更点としてブラウザ操作改善、セキュリティ強化、fast mode、ダッシュボード刷新などを要約して共有した。
- Discord アクセス設定メモ: ユーザーから guild `1479139352625021019` でも反応させたい要望あり。現状は許可ギルド外で反応しない。設定追加の話が出ており、今後 `channels.discord.guilds` への追加作業が必要になる可能性あり。
