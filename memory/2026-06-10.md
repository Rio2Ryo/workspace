# 2026-06-10

## 08:50 JST — Shiro Loop 承認待ち分類の改善
- 何が起きたか: Yakonより「全件承認待ちなん？全然わからない」と指摘。Shiro Loop が 14 件をすべて `permission-wait` として扱い、Yakon判断待ち・外部操作待ち・完了サイン待ち・Shiro自走対象・他Director依頼対象が同じ表示に潰れていた。
- なぜ起きたか: tmux watcher の状態分類が機械的で、人間が次に判断すべき粒度へ再分類していなかった。通知ノイズ削減を優先した結果、判断キューとしての可読性が不足した。
- 次回どう防ぐか: `tmux-web-view` に読み取り専用の「判断キュー」を追加し、tmux出力から `Yakon判断 / 外部操作 / 完了サイン / Shiro自走 / 他Director依頼` に自動分類する。承認実行ボタンは入れず、まず判断材料・リスク・次アクションを集約する。
- ルール反映: 「承認待ち」と報告する場合は、必ず何の承認か、誰が動くか、外部変更/課金/削除/本番影響の有無、推奨案を添える。単なる `permission-wait` 件数だけを人間向け報告に使わない。

## 10:40 JST — tmux-web-view 判断キュー本番反映
- Yakonの「デプロイまでお願いします。あとループもお願いします」を受け、`tmux-web-view` に判断キューUIを追加して Vercel production へ反映。
- Production alias: `https://tmux-web-view.vercel.app`
- Deployment URL: `https://tmux-web-view-9vjt1fp7o-commongiftedtokyo.vercel.app`
- 確認: local `npm run build` pass、Vercel build pass、公開URLのHTMLに `判断キュー / Yakon判断 / 外部操作 / 完了サイン` が含まれることを確認。
- Loop側: `ops/shiro-loop-tick.mjs` が `decisionKind / risk / reason / next` を state/report に保存するよう更新。`ops/shiro-loop-digest.mjs` も `decisionSummary` と分類済み `attention` を出すよう更新。実行結果は `permission-wait: 14` に加え、`external-action: 12 / done-signoff: 2` を確認。
# 2026-06-10

## 08:50 JST — Shiro Loop 承認待ち分類の改善
- 何が起きたか: Yakonより「全件承認待ちなん？全然わからない」と指摘。Shiro Loop が 14 件をすべて `permission-wait` として扱い、Yakon判断待ち・外部操作待ち・完了サイン待ち・Shiro自走対象・他Director依頼対象が同じ表示に潰れていた。
- なぜ起きたか: tmux watcher の状態分類が機械的で、人間が次に判断すべき粒度へ再分類していなかった。通知ノイズ削減を優先した結果、判断キューとしての可読性が不足した。
- 次回どう防ぐか: `tmux-web-view` に読み取り専用の「判断キュー」を追加し、tmux出力から `Yakon判断 / 外部操作 / 完了サイン / Shiro自走 / 他Director依頼` に自動分類する。承認実行ボタンは入れず、まず判断材料・リスク・次アクションを集約する。
- ルール反映: 「承認待ち」と報告する場合は、必ず何の承認か、誰が動くか、外部変更/課金/削除/本番影響の有無、推奨案を添える。単なる `permission-wait` 件数だけを人間向け報告に使わない。

## 10:40 JST — tmux-web-view 判断キュー本番反映
- Yakonの「デプロイまでお願いします。あとループもお願いします」を受け、`tmux-web-view` に判断キューUIを追加して Vercel production へ反映。
- Production alias: `https://tmux-web-view.vercel.app`
- Deployment URL: `https://tmux-web-view-9vjt1fp7o-commongiftedtokyo.vercel.app`
- 確認: local `npm run build` pass、Vercel build pass、公開URLのHTMLに `判断キュー / Yakon判断 / 外部操作 / 完了サイン` が含まれることを確認。
- Loop側: `ops/shiro-loop-tick.mjs` が `decisionKind / risk / reason / next` を state/report に保存するよう更新。`ops/shiro-loop-digest.mjs` も `decisionSummary` と分類済み `attention` を出すよう更新。実行結果は `permission-wait: 14` に加え、`external-action: 12 / done-signoff: 2` を確認。

## 15:00 JST — Shiro Loop digestの分類報告
- 15:00 JST digestでは total 14 / `permission-wait` 14、Changedなし。
- 分類済みattentionとして `high-risk external-action` 承認待ち12件、DONE確認待ち2件を報告。変化がないため未変化の個別一覧は省略した。
- 次回以降も、未変化のpermission-waitを大量列挙せず、分類サマリーと新規/変化/要対応だけを通知する。

## 21:23 JST — Shiro Loop verifier distill
- Verifier summary: {"external-action":13,"done-signoff":1} / verdicts {"needs-refine":14}.
- Learned: Never present raw permission-wait counts without decisionKind, risk, reason, and next action. / Before asking Yakon, verifier must confirm the item is external-action, needs-yakon, or done-signoff. / If verifier marks canProceedWithoutYakon=true, Shiro should run one low-risk local action before reporting.

## 21:32 JST — Agent Loop方式をShiro Loopへ適用
- Yakonが共有したX動画の要点（Fail -> Investigate -> Verify -> Distill -> Consult、独立Verifier、環境フィードバック、cross-session memory）をShiro Loopへ反映。
- `ops/shiro-loop-verify.mjs` を追加し、30分tick後に別視点で `decisionKind / risk / reason / next` を検証する構成へ変更。検証結果は `state/shiro-loop/last-verifier-report.json`、`learned-rules.json`、各sessionの `verifier` 欄、`events.jsonl` に保存する。
- `ops/shiro-loop-digest.mjs` に `verifierSummary` を追加し、定時digestで raw permission-wait 件数だけでなく、検証結果・ズレ・self-action候補を見られるようにした。
- cron更新: `shiro-loop-tick` は `tick -> verify` を実行、`shiro-loop-digest` は `decisionSummary / verifierSummary / verifier mismatch / self-action` を含めて要点通知する指示へ変更。
- 初回検証では `shiro-real-estate` の `done-signoff low` 判定を `external-action high` 候補として検出。全14件で `next` が汎用的すぎるため、次の改善対象は「各sessionごとの具体的な承認パケット / 自走可能アクションへの分解」。
